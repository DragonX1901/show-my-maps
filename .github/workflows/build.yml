name: Build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: ./gradlew build --stacktrace

      - uses: actions/upload-artifact@v4
        with:
          name: jar
          path: build/libs/*.jar

  gametest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      # The tests drive a real client, which needs a display and a GL stack.
      - name: Install display and GL packages
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libgl1-mesa-dri mesa-utils

      - name: Run client gametests
        run: xvfb-run -a ./gradlew runClientGameTest --stacktrace

      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: build/run/clientGameTest/screenshots/*.png
          if-no-files-found: ignore
